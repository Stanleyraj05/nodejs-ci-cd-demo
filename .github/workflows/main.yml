name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

 from a non TTY device` indicates that the Docker login    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Node.js
 command is attempting to perform an interactive login        uses: actions/setup-node@v3
        with:
          node-version: '14'

      - name: Install Dependencies
, which is not supported in the current environment        run: npm install

      - name: Run Tests
        run: npm test

      - name: Docker Login
        uses: docker/login-action@v2
        with.

To resolve this issue, you can modify the Docker:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      Login step to use the `docker/login-action` action - name: Build Docker Image
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/nodejs instead. This action is designed to handle Docker-demo-app:latest .

      - name: Push Docker Image
        run: docker push ${{ secrets.DOCKER_USERNAME }}/nodejs-demo-app:latest
